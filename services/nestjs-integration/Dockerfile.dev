FROM node:18-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy source
COPY . .

# Expose port
EXPOSE 3000

# Start in development mode (compiles TypeScript on the fly)
CMD ["npm", "run", "start:dev"]
